# 更新日志

本文件记录各版本的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

（暂无）

## [0.4.0] - 2026-09-12

### Added

- **界面外观改为玻璃拟态**：窗口与控件使用玻璃拟态材质，提供「深色 / 浅色 / 跟随系统」三档
  （Windows 下启用亚克力背景；「跟随系统」随系统深浅色自动刷新），与画布配色解耦
- **页面背景图**：
  - 攻略节点右键「从图片新建页面…」：以图片原始尺寸建页，并把图片复制进项目后作为整页底图
  - 页面节点右键「设置背景图…」/「清除背景图」；图片与页面比例不符时可选择是否同步调整页面尺寸
  - 按「等比覆盖 + 居中裁剪」铺满整页，画布、导出 PNG、文章页面引用、剪贴板复制结果一致
- **卡片边框支持四种形状**：矩形（圆角）/ 正方形 / 圆形 / 椭圆；正方形与圆形按内接绘制，
  选择后组件尺寸自动归一为 1:1
- **卡片边框支持框内图片**：框内显示、框外按形状裁掉，可调水平 / 垂直取景位置
- **署名水印样式设置**：导出对话框「添加作者署名」旁新增「署名设置…」按钮，可配置
  - 水印位置：左上 / 右上 / 左下 / 右下（默认右下）
  - 字体：字体族、字号、加粗
  - 颜色与不透明度（默认黑色 63%，与旧版水印一致）
  - 设置即时预览（复用导出绘制路径，所见即所得），并持久化为全局设置
- **可复用字体设置组件** `ui/FontSelectWidget`：字体族 / 字号 / 加粗 / 颜色 / 不透明度 + 预览，
  署名设置与文本样式共用同一组件
- **插入文本可直接设置样式**：`插入 → 文本` 改为弹出「文本样式」对话框（内容 + 对齐 + 字体 + 字号 +
  加粗 + 颜色 + 不透明度），不再是只能输入内容的单行输入框
- **文本组件支持不透明度**：`TextData` 新增不透明度（旧项目文件缺字段时按 100 处理，显示效果不变）
- **项目 Wiki**：新增 `wiki/` 使用教程（快速开始 / 界面 / 项目 / 画布 / 素材 / 模板 / 导出 / 文章 /
  设置 / 常见问题）与开发者文档（构建 / 架构 / 数据格式 / 插件 / 打包发布 / 贡献）

### Changed

- **画布配色精简为单一「浅色页面」**：移除旧的多主题画布配色，界面深浅色改由独立的
  「界面外观」设置负责；画布配色只影响页面内的主色 / 辅色 / 背景色 / 文字色
- **「关于」对话框文案重写**：补入文章攻略（Markdown 编辑 / 页面引用）与多格式导出（PNG / PDF /
  Markdown）、作者署名、MIT 许可与运行时 Qt 版本；介绍中不再点名具体发布平台
  （状态栏「复制到剪贴板」提示、设置页作者署名占位符同步改为通用表述）
- **版本号来源统一**：主程序版本改由 `CMakeLists.txt` 的 `project(VERSION)` 经 `BWM_VERSION` 宏注入，
  消除 `main.cpp` 双处硬编码；版本号自 0.1.0 对齐至 0.3.0
- **README 与文档入口调整**：移除「截图」占位栏，新增「使用教程（Wiki）」；原「文档」栏改为
  「设计文档」；「下载与发布」按实际产物修正（发布产物为单文件 `BWM.exe`）并去掉平台点名
- **素材路径逻辑收敛**：外部图片复制进项目 `assets/` 的实现统一收敛到 `project/AssetStore`
  （图片组件、素材面板导入、页面背景图三处共用）
- 署名水印样式经 `PluginContext` 传给导出 Provider，`IExportProvider` 接口签名不变（插件向后兼容）
- 页面 PNG 与文章 PNG 水印支持四角位置及字体 / 字号 / 颜色 / 不透明度；PDF 署名行套用字体 / 字号 / 加粗 / 颜色
- 默认值等价旧版硬编码水印（右下角、微软雅黑 18px、黑色 63%），未配置过的用户导出结果不变
- 「双击编辑文本」与「插入文本」统一使用同一个「文本样式」对话框（删除原先两套不一致的 UI）；
  未改动对齐方式时保留原有对齐值，历史项目中的文本排版不会因编辑而跳变
- 文本默认字体来源收敛到 `textDefaultFontFamily()`（原硬编码在绘制代码里），绘制与设置界面共用
- 移除历史打包脚本 `deploy.ps1` 与无引用的截图占位文件（现行流程为 `package.ps1` → `package_single.ps1`）

### Fixed

- 无（本次新增功能的开发期问题已随对应提交一并修复，未影响已发布版本）

### 升级影响

- 向下兼容：是（旧 `.bwm` 项目可直接打开；新增字段缺失时按默认值处理，显示效果与旧版一致）
- 如需手动操作：无

### 已知问题

- 崩溃恢复提示通常不会触发：保存使用 `QSaveFile`（临时文件名 `project.json.<随机字符>`），
  与恢复逻辑查找的 `project.json.tmp` 不一致，请以手动保存为准
- 图片组件保存的是素材绝对路径，项目文件夹移动 / 改名后可能失效（页面背景图与卡片边框图不受影响）
- 删除页面后，文章中的 `![[W:P]]` 引用不会自动修正，也没有越界提示
- 自动保存间隔修改后重启会回到默认 5 分钟
- 文章导出模式会残留一行空的「分辨率倍率」标签
- 页面长图 / 文章长图超过 30000px 时提示拆分导出（未自动分片）
- Markdown 导出不追加署名（保持原文纯净）
- 首次启动单文件 exe 略慢（虚拟文件系统初始化）

## [0.3.0] - 2026-09-09

### Added

- **文章攻略模块**：在攻略下新建 Markdown 文章（与页面同级）
  - 分栏编辑器：左侧 Markdown 源码、右侧实时预览（300ms 防抖）
  - 工具栏：加粗 / 斜体 / 标题 / 列表 / 链接 / 图片 / 页面引用
  - 页面引用 `![[W:P]]`：预览与导出时渲染为对应图文攻略页面的图片
  - 图形化页面选择器：无需手输索引
  - 从 `.md` 文件导入文章（本地图片自动复制进项目 `assets/`）
  - 导出三种格式：Markdown（`.md` + 兼容版 + `images/`）、PNG 长图、PDF 分页
  - 作者署名：PNG 右下角水印、PDF 文末署名行
- **应用图标**：窗口、任务栏、exe 文件图标（Chtholly.ico）
- **导出目录记忆**：导出对话框默认填充上次导出目录
- **单文件打包**：`package_single.ps1` 生成免安装的 `BWM.exe`（无需 Qt DLL）

### Fixed

- 修复菜单「导出 PNG」文字（现支持多格式，改为「导出」）
- 修复浅色风格下工具栏文字仍为白色（`QToolButton` 不继承 `QToolBar` 的 `color`）
- 修复插入工具栏在菜单栏任意位置右键都弹出切换菜单（现仅「插入」菜单项右键生效）
- 修复文章预览 Ctrl+滚轮只缩放文字、图片不跟随（现整体缩放并保持滚动位置）
- 修复文章导出缺少作者署名（接口缺少 `strAuthor` 参数）
- 修复文章 PNG 长图缺少高度上限检测（超长文章静默失败）
- 修复深色主题下文章导出文字不可见（Markdown 默认黑字）
- 修复文章长图字号过小（改为按图片宽度比例缩放）
- 修复 `package.ps1` 语法错误与 Qt 路径误取 MSVC 版环境变量

### Changed

- 插入工具栏默认隐藏（右键「插入」菜单项可切换显示）
- 文章归属调整：文章改为攻略的子级（原设计与图文攻略平级），旧文件自动迁移
- 导出对话框按选中对象自动切换文章 / 页面模式
- 新增 `Qt6::PrintSupport` 依赖（PDF 导出）

### 升级影响

- 向下兼容：是（旧 `.bwm` 项目可直接打开，project 级文章自动迁移到攻略内）
- 如需手动操作：无

### 已知问题

- 文章 PNG 长图超过 30000px 时提示拆分导出（未自动分片）
- Markdown 导出不追加署名（保持原文纯净）
- 首次启动单文件 exe 略慢（虚拟文件系统初始化）
